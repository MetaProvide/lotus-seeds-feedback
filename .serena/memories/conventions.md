# Conventions

- Submission types are restricted to `feature`, `improvement`, and `bug` in the public payload.
- The target workflow maps those types to labels `request`, `enhancement`, and `bug` respectively; it also adds `seeds`.
- Use submitted feedback title verbatim for GitHub issue titles; type context remains in the issue body.
- Bound external user-provided fields before sending them to GitHub.