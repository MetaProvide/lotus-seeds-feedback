# Task Completion

- Run `git diff --check` in each modified repository.
- For changes to `../lotus/.github/workflows/seeds-feedback-issue.yml`, run Ruby YAML parsing from that repository.
- Manually verify function payload fields and target workflow mappings together when either contract changes.